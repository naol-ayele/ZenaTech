import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads successfully', (WidgetTester tester) async {
    // Placeholder test - integration tests require mocking providers
    expect(true, isTrue);
  });
}
